apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sdvdemo
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - backend:
          service:
            name: frontend-service
            port:
              number: 8888
        path: /
        pathType: Prefix